{
  "name": "crawlstr-v2",
  "private": true,
  "version": "2.0.0",
  "license": "MIT",
  "description": "Crawlstr v2 — decentralized browser crawler for the SIP-01 index (self-contained app repo)",
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "postinstall": "pnpm --filter @sip01/protocol run build",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": { "node": ">=20" }
}
